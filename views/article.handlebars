<div class="text-center">
  <h2>{{name}}</h2>
  <h4>{{description}}</h4>
  <p>{{story}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
    <button class="btn btn-primary">Share this Article!</button>
  </p>
</div>
